export default function GeminiChat({ height }) {
  return (
    <div
      style={{
        height,
        background: "#fff",
        flexShrink: 0,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#ccc",
        fontFamily: "monospace",
        fontSize: 12,
        letterSpacing: "0.08em",
      }}
    >
      [ GeminiChat ]
    </div>
  );
}
